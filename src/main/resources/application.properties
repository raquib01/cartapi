spring.application.name=cartapi

spring.datasource.url=jdbc:mysql://localhost:3306/cartapi?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=sql123

spring.jpa.show-sql=true

spring.liquibase.enabled=false
spring.change-log=classpath:db/changelog/db.changelog-master.yaml

spring.jwt.secret = ${JWT_SECRET_KEY}
# 24hrs: 86400 seconds
spring.jwt.accessTokenExpiration = 86400
# 7d: 604800 seconds
spring.jwt.refreshTokenExpiration = 604800
stripe.secretKey = ${STRIPE_SECRET_KEY}
stripe.webhook.secretKey = ${STRIPE_WEBHOOK_SECRET_KEY}
websiteUrl=http://localhost:3000
